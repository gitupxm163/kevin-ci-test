apiVersion: v1
kind: Service
metadata:
  name: bar-service
  labels:
    app: bar-echo
spec:
  selector:
    app: bar-echo
  ports:
  - port: 80
    targetPort: 5678
    protocol: TCP
  type: ClusterIP