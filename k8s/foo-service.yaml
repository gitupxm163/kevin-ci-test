apiVersion: v1
kind: Service
metadata:
  name: foo-service
  labels:
    app: foo-echo
spec:
  selector:
    app: foo-echo
  ports:
  - port: 80
    targetPort: 5678
    protocol: TCP
  type: ClusterIP